{# <div class="section-head" data-trigger="{{ content.heading|lower|replace({' ': '-'}) }}" id="{{ content.heading|lower|replace({' ': '-'}) }}"> #}
	
{# 	{% if content.align %}
		{% set alignment = 'rightblock' %}
	{% else %}
		{% set alignment = 'lefttblock' %}
	{% endif %} #}



	<div class="inner">
		<div class="content-block subtext">
			{{ content.sub_text|markdown }}			
		</div>
		<div class="content-block maintext">
			{{ content.text1|markdown }}			
		</div>

	</div>
{# </div> #}